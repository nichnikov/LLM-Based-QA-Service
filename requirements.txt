# --- Основные зависимости приложения ---

# Веб-фреймворк для создания API
fastapi==0.115.7

# ASGI-сервер для запуска FastAPI
uvicorn[standard]==0.34.0

# Клиентская библиотека для работы с OpenAI-совместимыми API
openai==1.60.1

# Библиотека для валидации данных и управления настройками
pydantic==2.8.2

# Расширение для Pydantic для загрузки настроек из .env файлов
pydantic-settings==2.7.1

# Библиотека для загрузки переменных окружения из .env файла (используется pydantic-settings)
python-dotenv==1.0.1

# Библиотека для выполнения асинхронных HTTP-запросов (используется в Retriever)
aiohttp==3.11.11

# Библиотека для синхронных HTTP-запросов. 
# Примечание: была в исходном проекте, но в рефакторинговой версии не используется напрямую.
# Может быть полезна для вспомогательных скриптов или отладки.
requests==2.32.3


# --- Зависимости для разработки и тестирования ---

# Фреймворк для написания и запуска тестов
pytest

# Плагин для pytest для поддержки асинхронных тестов (asyncio)
pytest-asyncio

# Плагин для pytest, который упрощает использование мок-объектов
pytest-mock